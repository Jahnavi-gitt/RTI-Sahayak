# Base URL of the RTI Sahayak FastAPI backend (see /backend).
# Leave unset for a frontend-only demo — the app then uses its
# built-in deterministic mock engine for every AI-powered step.
VITE_API_BASE_URL=http://localhost:8000
